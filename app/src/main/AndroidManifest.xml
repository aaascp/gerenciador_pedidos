<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.aaascp.gerenciadordepedidos">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".presentation.ui.main.MainActivity">

            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <activity android:name=".presentation.ui.order.list.OrdersListActivity" />

        <activity android:name=".presentation.ui.order.details.OrderDetailsActivity" />

        <activity android:name=".presentation.ui.order.details.OrderMoreDetailsActivity" />

        <activity android:name=".presentation.ui.camera.BarcodeProcessorActivity" />

    </application>

</manifest>